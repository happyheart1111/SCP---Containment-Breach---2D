# 2026-08-09 工作日志 (续)

## v1.0.0 增量优化 — 开始菜单/Tab背包/SCP AI/UI布局

在 v1.0.0 基础上,用户提出 4 项增量优化,全部完成:

### 1. 加入开始菜单 (任务9)
- 新增 `#start-menu` 标题界面(标题光晕 + CONTAINMENT BREACH 副标题 + 开始游戏/观察模式按钮)
- 加载默认显示 start-menu,点击"开始游戏" → 显示 `#role-menu` 角色选择
- "观察模式"按钮 → `startObserve()`: 不创建玩家, AI 自主交战
- `_showMenu()` 回到 start-menu (之前是 role-menu)
- `_showRoleMenu()` 专门显示角色选择

### 2. 优化行为提示界面位置 (任务10)
- 原 `#hud-controls`(底部中央遮挡画面) → 改为右下角竖排可折叠面板
- 折叠按钮 ⚙ 默认收起(只显示齿轮图标,旋转45度悬停效果),展开时显示速度/显示/操作按钮
- 事件日志 `#hud-bottom` 从底部中央 → 右下角,半透明(0.8),悬停加亮
- 背包面板 `#inventory-panel` 右上角,默认隐藏
- 中央游戏画面完全无 UI 遮挡

### 3. Tab 背包开关 (任务11)
- HTML: 新增 `#inventory-panel` 覆盖层(右上角, 默认 hidden)
- 6 格固定槽位, 显示物品图标 + 名称 + 数量计数
- 悬停格子选中 → 显示详情面板(名称/描述/使用提示)
- 点击格子 → 使用该物品
- 数字键 1-6 直接使用(原功能保留)
- 死亡/返回菜单时自动关闭背包

### 4. 优化 SCP AI 逻辑 (任务12)
- **SCP-173 眨眼机制**: SCP原设定! 周期眨眼(5-8s)持续 0.6s, 眨眼瞬间无视注视可移动
  - 玩家版也加眨眼机制: HUD 显示 "👁 眨眼! 可移动" 金色提示
  - 渲染: 眨眼中显示金色脉冲光环
- **SCP-173 目标选择**: 优先武装目标(威胁大), 已交战目标降权, 远距离 A* 寻路, 近距离直线扑杀
- **SCP-049 智能猎杀**: 优先无武器平民(更容易制造僵尸), A* 接近, 仪式感停顿
- **SCP-939 真正伏击**: 听到声音 → 潜行(55速度)接近 → 到达 < 70px 后静止埋伏 → 猎物接近 < 90px 扑击, 扑击后停顿 1.5s

### 关键实现位置
- `prototype/index.html`: `#start-menu`, `#inventory-panel`, `#hud-controls` 结构调整
- `prototype/css/style.css`: `.start-box/.start-title/.start-buttons`, `#inventory-panel/.inv-slot`, `#hud-controls` 右下角 + 折叠动画, `#hud-bottom` 右下角
- `prototype/js/game.js`: `startObserve()`, `_showMenu()`/`_showRoleMenu()`, Tab 监听, `_setInventoryPanel()`, `_renderInventoryPanel()`, 173 blink 状态显示
- `prototype/js/npc.js`: NPC 构造加 blink 字段, 重写 `_behaviorSCP173/049/Ambush`
- `prototype/js/player.js`: 玩家版 SCP-173 加眨眼机制
- `prototype/js/renderer.js`: `_drawPlayer` 加 gameTime 参数, 173 blink 视觉

### 测试
- 8 套测试全绿: jsdom (20+项)/ headless / interaction / crosslevel (4 个 NPC 跨图) / connectivity (5次随机全连通) / facilities / scp_ai (4项全 PASS) / dist
- scp_ai 测试结果: 173 眨眼(12s内 77 帧) / 049 优先无武器 PASS / 939 扑击 PASS / 173 注视冻结+眨眼移动 PASS

### 文件结构
- screenshots/v11_start.png — 新开始菜单(标题光晕 + 开始游戏按钮)
- screenshots/v11_role_menu.png — MTF 游戏画面, 显示新布局: HUD顶栏/任务面板左上/玩家HUD左下/折叠控件右下(⚙)/事件日志右下/阵营右上, 中央画面无 UI 遮挡

### 截图核心亮点
- 开始菜单: 巨大标题"SCP 收容失效"带红色光晕, "CONTAINMENT BREACH" 副标题, 主按钮"开始游戏"蓝色高亮
- 控制面板: 默认折叠只显示 ⚙ 圆形按钮(右下角), 不占屏幕空间
- 游戏画面完全干净: 没有任何UI遮挡中央视野